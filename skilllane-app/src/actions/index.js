import bases from '../apis/bases'
import history from '../history'

// export const fetchUsers = () => async (dispatch) => {
//     const response = await bases.get("/");
//     dispatch({ type: 'FETCH_USER', payload: response.date})
// }